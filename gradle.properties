# Recommended for modern Gradle (2026 best practices)
org.gradle.configuration-cache=true               # Preferred mode – big speedup!
org.gradle.configuration-cache.problems=warn      # Helpful during initial migration
org.gradle.parallel=true                          # Run tasks in parallel
org.gradle.caching=true                           # Reuse task outputs
org.gradle.jvmargs=-Xmx4g -XX:MaxMetaspaceSize=512m  # More memory for large builds
